import { findByRole } from '@testing-library/react'
import React from 'react'
import ForecastItem from './ForecastItem'
import { render } from '@testing-library/react'

test('ForecastItem render', async () => {
 /*   const { findByText } = render(<ForecastItem weekDay='Lunes' hour={10} state='Soleado' temperature={28}  />)

    const weekDay= await findByText(/10/)

    const hour= await findByRole("heading")

    const state= await findByText('Soleado')

    const temperature= await findByRole(28)


    expect(weekDay).toHaveTextContent("Dia: 10")
    expect(hour).toHaveTextContent(10)
    expect(state).toHaveTextContent("Lunes")
    expect(temperature).toHaveTextContent(28)
*/
    
})